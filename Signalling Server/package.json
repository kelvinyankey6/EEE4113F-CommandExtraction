{
  "name": "pi_signalling",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx rm -rf ./build && npx tsc && node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/ws": "^8.5.4",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "ws": "^8.13.0"
  }
}
